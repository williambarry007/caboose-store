<!DOCTYPE>
<html>
  <body>
    <script id="response">
      parent.postMessage({
        success: <%= @success %>,
        message: '<%= @message %>'
      }, '<%= CabooseStore::root_url %>');
    </script>
  </body>
</html>

